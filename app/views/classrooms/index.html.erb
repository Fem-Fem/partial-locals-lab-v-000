<%= form_tag("/index") dp %>
  String: <%= text_field_tag "string" %>
  <%= submit_tag "Submit String"
<% end %>

<% @classroom.students.each do |student| %>
  <%= render partial: "students/student", locals: {post_student: student} %>
<% end %>
